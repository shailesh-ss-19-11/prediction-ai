# Copy to .env and fill in real values. .env is gitignored — never commit it.

# Jev AI (https://jev-ai.pro) — used by news_fetcher.assess_urgent_risk()
# when config.USE_AI_ENGINE = True.
JEV_AI_API_KEY=
